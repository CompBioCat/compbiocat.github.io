# Quarto build output — never committed to main; CI renders and publishes to gh-pages
_site/
.quarto/
*.html.md

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors
.vscode/
.idea/
*.swp

# Python
__pycache__/
*.pyc
.venv/
venv/

# Image build scratch — the intake originals ARE committed (they are the source
# of truth for scripts/build_images.py), but never any OS or editor sidecars.
assets/images/**/.DS_Store

/.quarto/
**/*.quarto_ipynb

# Working documents, not site content. The content brief and the project
# kickoff notes are the group's internal working material and stay on the
# local machine; this repository is public.
content/
